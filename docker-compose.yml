version: '3.8'

services:
  trace-analyzer:
    build: .
    ports:
      - "5000:5001"
    volumes:
      - ./uploads:/tmp
    environment:
      - FLASK_ENV=production
      - FLASK_DEBUG=0
    restart: unless-stopped
